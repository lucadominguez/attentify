'use strict';
// TEMPLATE — copy to config.js (gitignored) and fill in the key. config.js ships
// inside the packaged extension so users get zero-setup AI, but the live key is
// never committed to this public repo.
self.ATTENTIFY_BUNDLED_OPENROUTER_KEY = 'sk-or-v1-REPLACE_ME';
